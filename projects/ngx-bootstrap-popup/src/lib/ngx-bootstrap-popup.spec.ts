import { NgxBootstrapPopup } from './ngx-bootstrap-popup';

describe('NgxBootstrapPopup', () => {
  it('should create an instance', () => {
    expect(new NgxBootstrapPopup()).toBeTruthy();
  });
});
